# Optional: offline embeddings for pinpoint campaign memory.
#
# Mythforge's deep memory prefers your LLM provider's own embeddings endpoint
# (Ollama: `ollama pull all-minilm` — zero Python deps). Install THIS only if
# you want local ONNX embeddings without an embeddings-capable model provider:
#
#   pip install -r requirements-optional.txt
#
# ~130 MB (fastembed + onnxruntime + the MiniLM model on first use).
fastembed

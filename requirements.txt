# Mythforge — core runtime dependencies (lean).
# Verified: the app boots without the vector-DB / embedding / calendar stack the
# old Odysseus platform required (chromadb, fastembed, onnxruntime, caldav,
# icalendar) — those were removed in the extraction (~240 MB of deps).

fastapi
uvicorn
python-multipart
python-dotenv
httpx
pydantic>=2.0
pydantic-settings>=2.0
SQLAlchemy
numpy                 # game music generation + pinpoint campaign-memory cosine search
pypdf                 # PDF upload in chat (vision)
beautifulsoup4
charset-normalizer
markdown
nh3                   # HTML sanitizer for untrusted rendered content
python-dateutil
youtube-transcript-api
cryptography
bcrypt
pyotp                 # 2FA
qrcode[pil]
croniter
mcp                   # still wired in app.py; slated for removal in a later pass
pytest
pytest-asyncio
